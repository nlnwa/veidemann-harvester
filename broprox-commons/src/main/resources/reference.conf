# Database connection parameters
dbHost="localhost"
dbHost=${?DB_HOST}

dbPort=28015
dbPort=${?DB_PORT}

dbName="broprox"
dbName=${?DB_NAME}

# OpenTracing parameters
tracerUri="http://tracer:9411/api/v1/spans"
tracerUri=${?TRACER_URI}
