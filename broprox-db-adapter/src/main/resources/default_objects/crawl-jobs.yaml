meta:
    name: "Default"
    description: "The default job definition"
    label:
        - key: "type"
          value: "default"
schedule:
    meta:
        name: "Every minute"
        description: "A schedule which triggers every minute"
    cronExpression: "* * * * *"
limits:
    maxDurationS: 60
crawlConfig:
    meta:
        name: "Default"
        description: "Default crawl configuration"
    browserConfig:
        meta:
            name: "Default"
            description: "Default browser configuration"
        windowWidth: 900
        windowHeight: 900
        pageLoadTimeoutMs: 10000
        sleepAfterPageLoadMs: 500
    politeness:
        meta:
            name: "Default"
            description: "Default politeness configuration"
        ignoreRobots: false
        minTimeBetweenPageLoadMs: 1000
    extra:
        extractText: true
        createSnapshot: true
    depthFirst: false